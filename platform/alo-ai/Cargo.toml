[package]
name = "alo-ai"
description = "alo AI inference layer: a model-agnostic client speaking the OpenAI-compatible Chat Completions API (Ollama/vLLM/hosted), configured per tenant"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
alo-store = { path = "../alo-store" }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
reqwest = { workspace = true }
# DNS resolution for the restricted egress policy (SSRF guard): resolve + pin
# the backend host before connecting.
tokio = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }

[lints]
workspace = true
