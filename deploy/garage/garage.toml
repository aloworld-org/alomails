# Garage configuration for the alo development/CI compose stack.
# Secrets come from the environment (GARAGE_RPC_SECRET,
# GARAGE_ADMIN_TOKEN in docker-compose.yml) — never from this file.
# Production topology (replication across two EU locations per the DR
# targets) is deploy work for ROADMAP.md Phase 2 "Backups per DR
# targets".

metadata_dir = "/var/lib/garage/meta"
data_dir = "/var/lib/garage/data"

replication_factor = 1

rpc_bind_addr = "[::]:3901"
rpc_public_addr = "127.0.0.1:3901"

[s3_api]
s3_region = "garage"
api_bind_addr = "[::]:3900"
root_domain = ".s3.alo.test"

[admin]
api_bind_addr = "[::]:3903"
