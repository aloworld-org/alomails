# Rspamd redis backend (local.d/redis.conf) — used by the Bayes
# classifier for spam/ham token storage, and later by greylisting and
# ratelimit. The redis service lives on the private compose network and
# is never published. Engine configured, never patched (CLAUDE.md).
servers = "redis:6379";
