# Rust
/target/

# Node
node_modules/
web/dist/

# Environment — real values never enter the repo (see deploy/.env.example)
.env
.env.*
!.env.example
deploy/.env

# Editor/OS noise
.DS_Store
Thumbs.db

# Local per-developer Claude Code permissions (never shared)
.claude/settings.local.json
