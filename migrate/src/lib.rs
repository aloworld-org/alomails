//! # alo-migrate — the M365 exit suite (not yet implemented)
//!
//! Future role (product doc §6, ROADMAP.md Phase 4): Graph-API tenant
//! audit with readiness score, identity import from Entra ID, mailbox
//! and calendar import (recurrences with exceptions, Teams-link
//! rewriting), OneDrive/SharePoint import with permission mapping,
//! autodiscover endpoints, dual-delivery cutover safety, and the
//! subscription-retirement wizard. Launch bar: a 30-person company can
//! cancel its entire M365 subscription the day after migrating.
//!
//! This crate is intentionally empty at Phase 0: it exists so the
//! workspace compiles whole and the monorepo shape from
//! ARCHITECTURE.md is real from the first commit.
